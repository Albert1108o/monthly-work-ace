# Copie para .env e ajuste se for usar um banco PostgreSQL hospedado na internet.
# Assim todas as máquinas usam o MESMO banco e o login funciona em qualquer uma.
DB_HOST=db
DB_PORT=5432
DB_NAME=horas
DB_USER=horas
DB_PASSWORD=horas
